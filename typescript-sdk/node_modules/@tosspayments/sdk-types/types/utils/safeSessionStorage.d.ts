declare class SafeSessionStorage {
    get(key: string): string | null;
    set(key: string, value: string): void;
    clear(): void;
}
declare const safeSessionStorage: SafeSessionStorage;
export default safeSessionStorage;
