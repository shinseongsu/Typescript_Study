import { OpenIframeOptions } from './openIframe';
import { PaymentParams } from '@connectpay/types';
export declare function requestPayment(customerToken: string, { successUrl, failUrl }: Pick<PaymentParams, 'successUrl' | 'failUrl'>, { container }: OpenIframeOptions): Promise<never>;
