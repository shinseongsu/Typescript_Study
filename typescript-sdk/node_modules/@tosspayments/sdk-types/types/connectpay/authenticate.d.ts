import { ConnectpayOptions } from '@connectpay/types';
import { OpenIframeOptions } from './openIframe';
export declare function authenticate(clientKey: string, customerKey: string, { container, redirectUrl, }: OpenIframeOptions & {
    redirectUrl: ConnectpayOptions['redirectUrl'];
}): Promise<string>;
